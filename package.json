{
  "name": "<project_id>",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "gulp build:oak && oak run --mode=dev",
    "dev:watcher": "nodemon --watch src/oak -e ts --exec npm run dev",
    "gulp": "gulp",
    "gulp:prod": "gulp build",
    "migratedb": "oak migratedb",
    "start": "oak run --mode=prod"
  },
  "engines": {
    "node": "14.x.x"
  },
  "dependencies": {
    "@oak/oak": "latest"
  },
  "devDependencies": {
    "colors": "^1.4.0",
    "esbuild": "^0.12.1",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^7.0.1",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^4.1.0",
    "nodemon": "^2.0.7",
    "sass": "^1.33.0",
    "typescript": "^4.2.3"
  }
}
